import React from "react";
import Header from "../components/Header";

const Home = () => {
        return (
                <div className="w-full h-screen bg-white">
                        <Header></Header>
                </div>
        );
};

export default Home;
